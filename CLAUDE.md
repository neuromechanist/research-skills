@AGENTS.md

## Claude Code Specific Instructions

The shared marketplace instructions live in `AGENTS.md`; this file imports them for Claude Code with `@AGENTS.md`.

Keep any Claude-only marketplace, plugin, skill, command, or MCP notes below this section so the cross-agent source of truth stays in `AGENTS.md`.
